sudo: false
dist: trusty
language: python

python:
  - "3.6"
  - "3.7"

env:
  - DJANGO="2.2"

services:
  - postgresql

addons:
  postgresql: "9.5"

install: pip install tox-travis coverage
script: tox

after_success:
  - bash <(curl -s https://codecov.io/bash)

notifications:
  email: false
